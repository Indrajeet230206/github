git is free and open source version control system

git --version
git -> it shows different commands

git config --global user.name "Indrajeet230206"
git config --global user.name "nadargeindrajeet@gmail.com"

git config --list

git clone https://github.com/Indrajeet230206/delta.git

//terminal
ls -> show all folders inside folder
ls -a   -> hidden files

git status

untrakced
these are new files -> add the file

unmodified (normal stage)
 -> edit the file

modified
 -> stage the file

staged
 <- commit (unmodified)


git add file_name
git .

git commit -m "some message"

git push origin main

git init -> initialize empty git repository

git remote add origin https://github.com/Indrajeet230206/github.git
git remote -v

git branch -M main -> to rename branch

git push -u origin main
now we can use only git push instead of git push orogin main

git commit -am "added dot" -> commit and add simultaneously

--------- branch commands ------------
git branch -> to check branch
git branch -M main -> to rename branch
git checkout -b branch_name  -> to create new branch
git checkout branch name  ->  to navigate
git branch -d branch_name  ->  to delete branch